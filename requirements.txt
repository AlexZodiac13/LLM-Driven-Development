# Core
pandas>=2.0
numpy>=1.24
tqdm>=4.66
psutil>=5.9

# HF datasets + models
datasets>=2.18
transformers>=4.41
accelerate>=0.30

# Torch (install the right build for your machine: CPU or CUDA)
torch

# Optional (only if you plan to run 4-bit quantization via bitsandbytes)
# bitsandbytes

# Optional (if some tokenizers require it)
# sentencepiece
